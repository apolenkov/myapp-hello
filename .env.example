# App
PORT=3001
NODE_ENV=development
APP_NAME=myapp-hello

# Auth (required for protected routes)
JWT_SECRET=changeme-use-a-strong-random-value

# Logging
# LOG_LEVEL=info

# Database (shared by app + postgres containers)
POSTGRES_USER=dev_user
POSTGRES_PASSWORD=changeme
DATABASE_URL=postgresql://dev_user:changeme@postgres:5432/myapp_dev

# OpenTelemetry (optional — enables OTLP trace export to Tempo)
# OTEL_EXPORTER_OTLP_ENDPOINT=http://tempo:4318

# Swagger (optional — defaults to http://localhost:3001)
# PUBLIC_URL=https://your-domain.com

# Grafana (required for observability stack)
# GRAFANA_ADMIN_USER=admin
# GRAFANA_ADMIN_PASSWORD=changeme
# GRAFANA_ROOT_URL=http://localhost:3100
